{
  "name": "expojet",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "expojet": "./packages/cli/dist/cli.js",
    "create-expojet": "./packages/cli/dist/cli.js"
  },
  "description": "A production-ready Expo stack in one command.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "turbo build",
    "changeset": "changeset",
    "check": "biome check . && pnpm sdk:check && turbo typecheck test",
    "format": "biome check --write .",
    "sdk:check": "node scripts/sync-sdk-pack.mjs --check",
    "sdk:sync": "node scripts/sync-sdk-pack.mjs",
    "smoke:pack": "node scripts/smoke-pack.mjs",
    "test": "turbo test",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@changesets/cli": "latest",
    "turbo": "latest",
    "typescript": "~6.0.3"
  }
}
