---
title: Uniwind
description: Utility-first styling optimized for Expo projects.
---

Use `--style uniwind` to generate the Uniwind adapter, its Metro integration, CSS entrypoint, and declarations.

```bash
npx create-expojet@latest my-app --style uniwind
```
