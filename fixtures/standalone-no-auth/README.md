# Stackjet Expo app

This project was generated from the checksum-verified Expo SDK 57 base pack.

```powershell
pnpm install
pnpm start
```

Open Expo Go and scan the QR code. This fixture has no authentication or server requirements.
